
public struct Map
{
    public readonly string name;

    public readonly int maxPlayers;

    public Map(string _name, int _maxPlayers)
    {
        name = _name;
        maxPlayers = _maxPlayers;
    }
}